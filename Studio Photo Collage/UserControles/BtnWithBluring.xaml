<UserControl
    x:Class="Studio_Photo_Collage.UserControles.BtnWithBluring"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Studio_Photo_Collage.UserControles"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Button x:Name="Btn"
            Width="120" Height="120"  
            Padding="0" Background="Transparent" BorderBrush="Transparent"
            HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
            <Grid>
                <Grid x:Name="ContentGrid"/>
                <Grid x:Name="Blur" Visibility="Collapsed"
                          Opacity="0.4" Background="Gray">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock FontSize="16" Text="{x:Bind ProjectName}"/>
                            <TextBlock FontSize="12" Text="{x:Bind Date}"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            <Interactivity:Interaction.Behaviors>
                <Core:EventTriggerBehavior EventName="PointerEntered">
                    <Core:ChangePropertyAction TargetObject="{Binding ElementName=Blur}" 
                                               PropertyName="Visibility" Value="Visible" />
                    <Core:ChangePropertyAction TargetObject="{Binding ElementName=Btn}"
                                               PropertyName="BorderBrush">
                        <Core:ChangePropertyAction.Value>
                            <SolidColorBrush Opacity="0.6" Color="{Binding Path=Color, Source={ThemeResource BlackAndWhite}}"/>
                        </Core:ChangePropertyAction.Value>
                    </Core:ChangePropertyAction>
                </Core:EventTriggerBehavior>
                <Core:EventTriggerBehavior EventName="PointerExited">
                    <Core:ChangePropertyAction TargetObject="{Binding ElementName=Blur}" 
                                               PropertyName="Visibility" Value="Collapsed" />
                    <Core:ChangePropertyAction TargetObject="{Binding ElementName=Btn}"
                                               PropertyName="BorderBrush">
                        <Core:ChangePropertyAction.Value>
                            <SolidColorBrush Color="Transparent"/>
                        </Core:ChangePropertyAction.Value>
                    </Core:ChangePropertyAction> 
                </Core:EventTriggerBehavior>
            </Interactivity:Interaction.Behaviors>
        </Button>
    </Grid>
</UserControl>
