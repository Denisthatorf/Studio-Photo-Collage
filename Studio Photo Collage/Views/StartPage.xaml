<Page
    x:Class="Studio_Photo_Collage.Views.StartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Studio_Photo_Collage.Views"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding StartPageInstance, Source={StaticResource Locator}}">
    <Grid>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Image Stretch="Fill" Source="/Assets/BackgroungStartPage.jpg"
                   />
        </Grid>
        <Grid>
            <Button HorizontalAlignment="Center" VerticalAlignment="Center" 
                    Command="{Binding ImageClickCommand}" Margin="200">
                <Image Source="/Assets/StartPageCentral.jpg"></Image>
            </Button>
            
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="50"
                       FontSize="30" Foreground="White">
                Welcome to Studio Photo Collages
            </TextBlock>
        </Grid>
    </Grid>
</Page>
